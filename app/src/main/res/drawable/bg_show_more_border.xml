<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="@android:color/transparent" />               <!-- light background -->
    <stroke
        android:width="1dp"
        android:color="@color/theme" />                   <!-- border color -->
    <corners android:radius="10dp" />               <!-- rounded corners / pill shape -->

    <!-- Optional: padding inside the shape -->
    <padding
        android:left="6dp"
        android:right="6dp"
        android:top="6dp"
        android:bottom="6dp" />

</shape>